/**
 * Contact page — skeleton only.
 * Use tokens: --token-* for colors, spacing, typography.
 */
export default function ContactPage() {
  return (
    <div>
      {/* Contact page skeleton */}
    </div>
  );
}
