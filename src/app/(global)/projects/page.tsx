/**
 * Projects list page — skeleton only.
 * Use tokens: --token-* for colors, spacing, typography.
 */
export default function ProjectsPage() {
  return (
    <div>
      {/* Projects page skeleton */}
    </div>
  );
}
