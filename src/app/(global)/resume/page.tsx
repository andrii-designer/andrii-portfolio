/**
 * Resume page — skeleton only.
 * Use tokens: --token-* for colors, spacing, typography.
 */
export default function ResumePage() {
  return (
    <div>
      {/* Resume page skeleton */}
    </div>
  );
}
